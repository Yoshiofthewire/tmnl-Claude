[Unit]
Description=TRMNL Claude Code Usage plugin
After=network.target

[Service]
Type=simple
# Adjust User and paths to your environment.
User=yoshi
WorkingDirectory=/home/yoshi/git/tmnl-Claude
# REQUIRED: the Claude Usage Dashboard API to pull from.
Environment=USAGE_API_URL=http://your-dashboard:8080
Environment=PORT=2523
Environment=TZ=America/New_York
# Quote values that contain spaces — systemd splits Environment= on spaces.
Environment="CLAUDE_PLAN=Claude Pro"
ExecStart=/usr/bin/node src/server.js
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
